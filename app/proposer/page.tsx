import ArtistForm from "@/components/ArtistForm";

export const metadata = {
  title: "Proposer un artiste — Carte des artistes",
};

export default function ProposerPage() {
  return <ArtistForm />;
}
